<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
        Declared so the app can be selected in Developer Options ›
        "Select mock location app". Android grants the underlying capability only
        after the user explicitly chooses this app there.
    -->
    <uses-permission
        android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="MockLocation,ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools" />

    <!--
        Required so the active mock session can run in a foreground service that
        keeps feeding Android the mocked coordinate after the app is backgrounded
        or closed — the fix for mocking silently reverting to real GPS.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Needed on API 33+ for the ongoing "Mock location active" notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="GPS Simulator"
        android:supportsRtl="true"
        android:theme="@style/Theme.RorkApp">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|layoutDirection|uiMode"
            android:theme="@style/Theme.RorkApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Owns the active mock-location session so it survives the app being
            backgrounded, navigated away from, or closed. See MockLocationService
            for why this exists and exactly what it guarantees.
        -->
        <service
            android:name=".location.MockLocationService"
            android:exported="false"
            android:foregroundServiceType="location"
            android:stopWithTask="false" />
    </application>

</manifest>
